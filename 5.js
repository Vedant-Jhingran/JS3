function calculateAreaofCircle(radius){
    let areaOfCircle = Math.round(Math.PI * Math.pow(radius,2))
    console.log(`Area of circle is ${areaOfCircle} units.`)
}

calculateAreaofCircle(3);
// OUTPUT: Area of circle is 28 units.